import { StyledLoading } from '../../styles/styledLoading';

export const Loading = () => {
	return (
		<StyledLoading role="loading">
			<span>.</span>
			<span>.</span>
			<span>.</span>
		</StyledLoading>
	);
};
